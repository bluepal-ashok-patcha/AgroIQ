server:
  port: 8083

spring:
  application:
    name: stream-intelligence-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
